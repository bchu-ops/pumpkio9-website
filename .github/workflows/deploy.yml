name: Deploy Frontend to GitHub Pages

on:
  push:
    branches:
      - main  # or master
  workflow_dispatch:   # 👈 Allows manual trigger from Actions tab

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout the repo
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2. Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      # 3. Navigate to frontend folder
      - name: Install frontend dependencies
        working-directory: ./frontend
        run: npm ci

      # 4. Build frontend
      - name: Build frontend
        working-directory: ./frontend
        run: npm run build

      # 5. Upload build output as artifact
      - name: Upload build artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./frontend/build   # build output folder

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      # # 5. Deploy build folder to GitHub Pages
      # - name: Deploy to GitHub Pages
      #   uses: peaceiris/actions-gh-pages@v3
      #   with:
      #     github_token: ${{ secrets.GITHUB_TOKEN }}  # Provided automatically
      #     publish_dir: ./frontend/build
      #     publish_branch: gh-pages        # Explicitly push to gh-pages branch
      #     # Optional:
      #     # user_name: "Brian Chu"
      #     # user_email: "brian.chu1030@gmail.com"
      #     # commit_message: "Auto deploy via GitHub Actions"